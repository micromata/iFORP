import { NavBarHeight } from '../../components/NavBar/NavBar';

export default theme => ({
  Whiteboard: {
    padding: '40px 80px',
    marginBottom: '80px',
    paddingTop: `${NavBarHeight}`
  }
});
